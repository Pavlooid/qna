- if current_user&.subscribes.any?
  = link_to 'Unsubscribe', subscribe_path(@subscribe), method: :delete, remote: true
- else
  = link_to 'Subscribe', question_subscribes_path(question), method: :post, remote: true
